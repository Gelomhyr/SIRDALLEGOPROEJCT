cd "File Location"

git init

git add. "if -> Asterisk * Lahat If kapag specific called the folder name or file name"

git commit -m "Commit or comment"

git remote add origin "https://github.com/Gelomhyr/SIRDALLEGOPROEJCT.git" <- Always ito

Pag mag pupush
git push -u origin main

pag mag pupull

git pull <- ganyan lang
